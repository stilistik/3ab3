endpoint: http://localhost:4466
datamodel:
  - models/common/client.prisma
  - models/common/user.prisma
  - models/common/post.prisma
generate:
  - generator: javascript-client
    output: ./generated/prisma-client/
seed:
  import: seed.graphql
hooks:
  post-deploy:
    - node ./seed.js
    - prisma generate
