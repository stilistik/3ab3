type Post {
  id: ID! @unique
  text: String!
  author: User! @relation(name: "PostAuthor", onDelete: SET_NULL)
  likedBy: [User!]! @relation(name: "LikedPosts")
  comments: [Comment!]! @relation(name: "PostComments", onDelete: SET_NULL)
  date: DateTime!
}
