type Payment {
  id: ID! @unique
  amount: Float!
  user: User!
  date: DateTime!
  verified: Boolean! @default(value: "false")
  transaction: Transaction! @relation(name: "PaymentTransaction" onDelete: CASCADE)
}
