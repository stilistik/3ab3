type Email @isAuthenticated{
  to: String!
  from: String!
  subject: String!
  text: String!
}

input EmailInput {
  to: String!
  from: String!
  subject: String!
  text: String!
}

extend type Mutation {
  sendEmail(input: EmailInput!): Email! @hasRole
}
