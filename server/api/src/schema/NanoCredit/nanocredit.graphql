type NanoCredit {
  id: ID!
  transaction: Transaction!
  amount: Float!
  user: User!
  description: String!
  date: DateTime!
}

input NanoCreditInput {
  amount: Float!
  description: String!
}

extend type Mutation {
  createNanoCredit(input: NanoCreditInput!): NanoCredit!
}
