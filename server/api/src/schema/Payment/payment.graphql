type Payment {
  id: ID!
  transaction: Transaction!
  amount: Float!
  user: User!
  date: DateTime!
}

input PaymentInput {
  userId: ID!
  date: DateTime!
  amount: Float!
}

extend type Query {
  payments: [Payment!]!
  payment(paymentId: ID!): Payment!
}

extend type Mutation {
  createPayment(input: PaymentInput!): Payment! @auth
  deletePayment(paymentId: ID!): Payment! @auth
}
