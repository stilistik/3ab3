type User {
  id: ID!
  email: String!
  name: String!
  posts: [Post!]!
}

input CreateUserInput {
  name: String!
  email: String!
}

extend type Query {
  users: [User!]!
}

extend type Mutation {
  createUser(input: CreateUserInput!): User
}
